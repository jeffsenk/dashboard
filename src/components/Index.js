import React from 'react';
import Base from './Base';

export default class Index extends React.Component {
  render(){
    return (
      <div className="home">
        <Base/>
      </div>
    );
  }
}
